# TODO
- [ ] Identify the exact runtime/build error (from terminal/console).
- [ ] Fix the React bug(s) in `src/pages/Home.jsx` preventing correct rendering / causing warnings.
- [ ] Ensure API import path matches (`Api.js` vs `api.js`).
- [ ] Fix MovieCard image field usage (`movie.url` vs TMDB fields).
- [ ] Run `npm run dev` (or `npm run build`) to confirm the error is resolved.

